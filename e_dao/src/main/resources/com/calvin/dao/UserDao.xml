<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.calvin.dao.UserDao">
    <resultMap id="userResult" type="User">
        <id column="uid" property="uid" javaType="Integer"/>
        <result column="username" property="username" javaType="String"/>
        <result column="password" property="password" javaType="String"/>
    </resultMap>
    <insert id="insert" useGeneratedKeys="true"  keyProperty="uid" parameterType="User" >
      insert into user(username,password)
      values (#{username,jdbcType=VARCHAR}  ,#{password,jdbcType=VARCHAR}  )
 </insert>

    <select id="selectById" resultMap="userResult" parameterType="int">
        select * from user where uid=#{uid}
    </select>

    <select id="selectByName" resultMap="userResult" parameterType="string">
        select * from user where username=#{username,jdbcType=VARCHAR}
    </select>

    <update id="update" parameterType="User">
        update user set password=#{password,jdbcType=VARCHAR}
        where uid=#{uid}
    </update>

    <delete id="delete" parameterType="int">
        delete from user where uid=#{uid}
    </delete>


    <select id="selectAll" resultType="User">
        select * from user
    </select>

    <select id="querySosCount" parameterType="int" resultType="int">
        select count(*)
        from sos where sos.uid=#{uid}
    </select>
</mapper>


