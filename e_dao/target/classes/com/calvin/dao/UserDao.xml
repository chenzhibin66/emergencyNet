<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.calvin.dao.UserDao">
    <resultMap id="userResult" type="User">
        <id column="username" property="username" javaType="String"/>
        <result column="password" property="password" javaType="String"/>
    </resultMap>
    <insert id="insert" parameterType="string">
      insert into user(username,password)
      values (#{username,jdbcType=VARCHAR}  ,#{password,jdbcType=VARCHAR}  )
 </insert>

    <select id="select" resultMap="userResult" parameterType="string">
        sselect * from user where username=#{username,jdbcType=VARCHAR}
    </select>

    <update id="update" parameterType="User">
        update user set password=#{password,jdbcType=VARCHAR}
    </update>

    <delete id="delete" parameterType="String">
        delete from user where username=#{account,jdbcType=VARCHAR}
    </delete>


    <select id="selectAll" resultType="User">
        select account from disaster_people
    </select>
</mapper>


