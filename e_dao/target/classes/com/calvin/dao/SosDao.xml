<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.calvin.dao.SosDao">
    <resultMap id="sosResult" type="Sos">
        <id column="sos_people" property="sosPeople" javaType="String"/>
        <result column="message" property="message" javaType="Integer"/>
        <result column="longitude" property="longitude" javaType="Double"/>
        <result column="latitude" property="latitude" javaType="Double"/>
        <result column="post_time" property="postTime" javaType="Date"/>

        <association column="poster" property="poster" javaType="User">
            <result column="account" property="username" javaType="String"/>
        </association>
    </resultMap>

    <insert id="insert" parameterType="Sos">
        insert into sos (sosPeople,message,longitude,latitude,postTime)
        values (#{sosPeople,jdbcType=VARCHAR} ,#{message} ,#{longitude} ,#{latitude} ,#{postTime,jdbcType=TIMESTAMP} );
    </insert>

    <select id="select" resultMap="sosResult" parameterType="String">
        select u.account,s.longitude,s.latitude,s.postTime
        from user u left join sos s
        where u.account=s.sosPeople
        and s.sosPeople=#{sosPeople,jdbcType=VARCHAR}
    </select>

    <select id="selectAll" resultMap="sosResult">
        select u.account,s.longitude,s.latitude,s.postTime
        from user u left join sos s
        where u.account=s.sosPeople
    </select>
</mapper>