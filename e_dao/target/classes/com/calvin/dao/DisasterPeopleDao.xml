<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.calvin.dao.DisasterPeopleDao">
 <insert id="insert" parameterType="string">
      insert into disaster_people(account,password)
      values (#{account,jdbcType=VARCHAR} ,#{password,jdbcType=VARCHAR}  )
 </insert>

    <select id="select" resultType="DisasterPeople" parameterType="string">
        select account,message,longitude,latitude
        from
        disaster_people
        where account=#{account,jdbcType=VARCHAR}
    </select>

    <update id="update" parameterType="DisasterPeople">
        update disaster_people set password=#{password,jdbcType=VARCHAR} ,
        message=#{message,jdbcType=VARCHAR} ,#{longitude} ,#{latitude},#{postTime,jdbcType=TIMESTAMP}
        where account=#{account,jdbcType=VARCHAR}
    </update>

    <delete id="delete" parameterType="String">
        delete from disaster_people where account=#{account,jdbcType=VARCHAR}
    </delete>


    <select id="selectAll" resultType="DisasterPeople">
        select account,message,longtitude,latitude,postTime
        from disaster_people
        desc by postTime
    </select>
</mapper>


